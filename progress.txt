## Codebase Patterns
- ExcelJS writeBuffer() requires `as unknown as Promise<Buffer>` cast to satisfy strict TypeScript generics
- Admin pages always gate on token URL param — show "Accesso Negato" if missing, same pattern across all admin pages
- Email functions use RESEND_FROM_EMAIL env var with fallback 'onboarding@resend.dev'; fire-and-forget with .catch() in routes
- Blob paths: config at 'config/valuation-parameters.json', streets at 'streets/<city-slug>.json'
- City slug: lowercase with hyphens (e.g. 'locate-di-triulzi') — use cityToSlug() from @/lib/cities
- ALLOWED_CITIES is the canonical city list in lib/cities.ts — import from there, never redeclare locally
- ValuationCoefficientTables has no index signature — use `as unknown as Record<string, unknown>` for dynamic key access
- admin/config/route.ts percentageFields Omit must exclude 'coefficienti' to avoid type error on numeric comparison

---

## 2026-06-02 - US-103
- Rewrote Step2View.tsx: removed ValuationPercentages, DEFAULT_PCT, legacy form fields (floor, hasElevator, hasSecondBathroom, hasCellar, exposure, buildYear, isRecentlyRenovated, heatingType). Added 9 new select fields (stato, classeEnergetica, annoCostruzione, ascensore, terrazzo, giardino, garage, cantina, riscaldamento)
- Summary card now shows tipologia/piano/locali/bagni from form1Data with readable labels (falls back to '—')
- Result screen now handles both string[] and {label, coefficiente}[] detail formats for forward/backward compat
- Rewrote Form2.tsx: removed ValuationPercentages import and /api/config fetch; updated Zod schema and default values; changed any types to unknown
- Files: components/valuation/Step2View.tsx, components/forms/Form2.tsx
- **Learnings for future iterations:**
  - Form2.tsx used `any` for state types — replaced with `unknown` + type assertions at Step2View call site
  - The `form1Data` prop in Step2View declares tipologia/piano/locali/bagni as optional strings — use '—' fallback for old sessions without these fields
---

## 2026-06-02 - US-102
- Added tipologia, piano, locali, bagni fields to Step1FormValues in Step1View.tsx (with PianoFormKey = Exclude<PianoKey, 'piano6Plus'>)
- Added 4 new select inputs in Step1View.tsx after squareMeters field
- Updated Zod schema and default values in Form1.tsx
- Updated app/api/form-1/route.ts: added 4 z.enum() fields, included them in response body
- Files changed: components/valuation/Step1View.tsx, components/forms/Form1.tsx, app/api/form-1/route.ts
- **Learnings for future iterations:**
  - PianoKey includes 'piano6Plus' (server-only aggregation) — form should use Exclude<PianoKey, 'piano6Plus'> to avoid Zod resolver type mismatch
  - SessionStorage restore already handled by form.reset(JSON.parse(stored)) in Form1.tsx — no special code needed for new fields
---

## 2026-06-02 - US-101
- Added 15 exported string union types to lib/config.ts (TipologiaCoefficientKey, StatoCoefficientKey, ClasseEnergeticaKey, AnnoCostKey, PianoSenzaAscensoreKey, PianoConAscensoreKey, PianoKey, LocaliKey, BagniKey, AscensoreKey, TerrazzoKey, GiardinoKey, GarageKey, CantinaKey, RiscaldamentoKey)
- Added ValuationCoefficientTables interface (14 coefficient tables) and added coefficienti field to ValuationConfig
- Added getCoefficienti() helper and implemented per-table deep merge in getValuationConfig()
- Updated lib/config.defaults.json with all 14 coefficient tables and their default values
- Fixed app/api/admin/config/route.ts: added 'coefficienti' to allowedKeys, excluded from percentageFields Omit
- Branch: ralph/v3-coefficienti (created from ralph/v2-municipalities-mvc)
- **Learnings for future iterations:**
  - ValuationCoefficientTables has no index signature — use `as unknown as Record<string, unknown>` for dynamic key loops
  - admin/config/route.ts Omit for percentageFields must exclude non-numeric fields (coefficienti) to avoid type errors
  - lib/config.defaults.json is cast `as ValuationConfig` — all new ValuationConfig fields need defaults in this file
---

## 2026-05-24 - US-001
- Created `lib/cities.ts` with `ALLOWED_CITIES` (7 new municipalities: Locate di Triulzi, Fizzonasco, Opera, Pieve Emanuele, Tolcinasco, Siziano, Carpiano) and `cityToSlug()` function
- Updated `components/valuation/Step1View.tsx`: removed local ALLOWED_CITIES, imported from @/lib/cities
- Updated `app/api/form-1/route.ts`: removed local ALLOWED_CITIES, imported from @/lib/cities, updated error message to use ALLOWED_CITIES.join(', ')
- Branch: ralph/v2-municipalities-mvc (created from master)
- **Learnings for future iterations:**
  - lib/streets/index.ts and app/api/admin/streets/route.ts do not exist on master — they'll be created in later stories and should import from @/lib/cities directly
  - The old cities (Milano, Monza, Sesto San Giovanni, Cinisello Balsamo) are replaced by the 7 new south-Milan municipalities
---
